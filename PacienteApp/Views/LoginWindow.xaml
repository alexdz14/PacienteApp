<Window x:Class="PacienteApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iniciar Sesión - Paciente" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Correo:"/>
            <TextBox x:Name="txtEmail" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Contraseña:"/>
            <PasswordBox x:Name="txtPassword" Width="300"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Iniciar Sesión"
                Width="150" Height="35" HorizontalAlignment="Center"
                Click="Login_Click"/>
    </Grid>
</Window>
